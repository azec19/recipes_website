{
  "type": "module",
  "dependencies": {
    "@apidevtools/swagger-parser": "^12.1.0",
    "@prisma/client": "^7.4.2",
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "express-validator": "^7.3.1",
    "file-type": "^21.3.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.0.2",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1",
    "pg": "^8.17.1",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "@prisma/adapter-pg": "^7.2.0",
    "@types/node": "^25.0.3",
    "@types/pg": "^8.16.0",
    "dotenv": "^17.2.3",
    "nodemon": "^3.1.11",
    "prisma": "^7.4.2",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "name": "recipes_website",
  "version": "1.0.0",
  "description": "This project follows the layered architecture pattern.\r # Presentation layer\r ## Data Transfer Objects (DTOs)\r DTOs are used to structure and standardize data exchange between Controllers and external clients.\r They are categorized in two categories:\r - Request DTOs: Define the expected structure of client requests.\r - Response DTOs: Define the structure of responses sent back to clients.\r One of the main reasons DTOs exists is to separate presentation logic from business logic. This allows\r for business updates without the need to update the whole API for a simple service layer change, as it\r allows for decoupling. Of course, API changes for valid reasons. These changes should be made and\r communicated properly.\r Be careful!\r Data Transfer Objects should only be used in the Controller layer. They are contracts that the\r client must follow in their requests and that the API must adhere to for its responses.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/azec19/recipes_website.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/azec19/recipes_website/issues"
  },
  "homepage": "https://github.com/azec19/recipes_website#readme"
}
